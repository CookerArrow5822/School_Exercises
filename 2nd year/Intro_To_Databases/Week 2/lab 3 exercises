-- lab 3 qs and solutions
-- qs 1-3 were a repeat of 7 - 9 from week 2

-- Q4. Show all films with rating 'PG' or 'PG-13', ordered by length (shortest first) and then title
SELECT title FROM film WHERE rating = "PG" or rating = "PG-13" ORDER BY length ASC, title ASC

-- Q5. List all distinct film ratings in the database, sorted alphabetically
SELECT DISTINCT rating FROM film ORDER BY rating ASC; 

-- Q6. List all films whose title starts with 'THE', ordered by title.
SELECT title FROM film WHERE title LIKE "THE%" ORDER BY title ASC

-- Q7. Get a list of cities in 'USA' where the city name contains the letter sequence 'New', ordered
by name. (Hint : use country code in city table)
SELECT Name from city WHERE CountryCode = 'USA' and Name LIKE "%New%" ORDER BY Name ASC

-- Q8. Find all cities whose name ends with ‘burg’ , ordered by population descending.
SELECT Name FROM city WHERE Name LIKE "%burg" ORDER BY Population DESC

-- Q9. Which distinct languages represent the majority language in at least one country? Display
ordered by percentage descending. (Hint: Use ‘countrylanguage’ table)
SELECT DISTINCT Language FROM countrylanguage WHERE Percentage > 50 ORDER BY Percentage DESC
